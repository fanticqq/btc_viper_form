//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit
import SnapKit

class {{ module_info.name }}ViewController: PSFormViewController, {{module_info.name}}ViewInput {

    fileprivate let dataDisplayManager: {{module_info.name}}DataDisplayManager

    var output: {{ module_info.name }}ViewOutput!
    override var formOutput: PSFormViewOutput {
        return self.output
    }    
    
    init(style: UITableViewStyle, dataDisplayManager: {{module_info.name}}DataDisplayManager)
    {
        self.dataDisplayManager = dataDisplayManager
        super.init(style: style)
    }
    
    required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    // MARK: {{module_info.name}}ViewInput
    override func setupInitialState() {
        self.setupTableView()
        self.setupDataDisplayManager()
        super.setupInitialState()
    }
}

// MARK: Configure
private extension {{ module_info.name }}ViewController {

	func setupTableView() {
        self.tableView.separatorStyle = .none
        self.tableView.backgroundColor = UIColor(named: .background)
	}

    func setupDataDisplayManager() {

    }
}


// MARK: Actions
private extension {{ module_info.name }}ViewController {

}


